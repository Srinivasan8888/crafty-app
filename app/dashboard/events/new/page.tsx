export default function NewEvent() {
  return (
    <div className="card p-8 text-center">
      <h1 className="text-2xl font-bold">Event create flow — coming next</h1>
      <p className="mt-2 max-w-xl mx-auto text-sm text-ink-muted">
        Event creation requires picking your organizer listing (crafter / store / studio).
        That UI ships in the next pass; the schema enforces the 3-FK CHECK from Issue 1.2.
      </p>
    </div>
  );
}
