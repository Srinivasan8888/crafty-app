export default function NewStudio() {
  return (
    <div className="card p-8 text-center">
      <h1 className="text-2xl font-bold">Studio create flow — coming next</h1>
      <p className="mt-2 max-w-xl mx-auto text-sm text-ink-muted">
        Studio onboarding ships in the next pass. The schema and APIs are ready; the form mirrors the crafter wizard with disciplines + age group.
      </p>
    </div>
  );
}
